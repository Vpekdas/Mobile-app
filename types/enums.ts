export enum FacilityType {
    HOSPITAL = "hospital",
    CLINIC = "clinic",
    OFFICE = "office",
    NONE = ""
}

export enum Sector {
    PUBLIC = "public",
    PRIVATE = "private",
    NONE = ""
}

export enum UserType {
    USER = "user",
    PROFESSIONAL = "professional"
}

export enum Sex {
    MAN = "man",
    WOMAN = "woman",
}