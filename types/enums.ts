export enum FacilityType {
    HOSPITAL = "hospital",
    CLINIC = "clinic",
    OFFICE = "office",
}

export enum Sector {
    PUBLIC = "public",
    PRIVATE = "private",
}

export enum UserType {
    USER = "user",
    PROFESSIONAL = "professional"
}

export enum Sex {
    MAN = "man",
    WOMAN = "woman",
}